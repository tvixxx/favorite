import { API_BASE_URL, APP_ENDPOINTS } from "@/constants/api/endpoints";

// Updated to match backend user endpoints structure
export const USERS_ENDPOINTS = `${API_BASE_URL}/${APP_ENDPOINTS.users}`; // Changed from /users to /user to match backend
