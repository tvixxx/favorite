# favourite

## Project setup

```
npm install
```

### Compiles and hot-reloads for development

```
npm run serve
```

### Compiles and minifies for production

```
npm run build
```

### Run your unit tests

```
npm run test:unit
```

### Lints and fixes files

```
npm run lint
```

### Customize configuration

See [Configuration Reference](https://cli.vuejs.org/config/).

### Feature

1) интеграция с IMDB для указания доп рейтинга у кино (сериала)
   **2) добавить при создании "страну" и сделать мульти жанры, а не только 1 как сейчас +**
   **3) Топ по фильмам и сериалам, по пользвоательской оценке фильмов и сериалов из базы (с возможностью фильтрации по
   актерам, жанрам, стране, по рейтингу, по дате выхода)**
4) Добавить возможность шаринга фильмов/сериалов в чате, и просто по ссылке (генерация) (*при вводе в тексте сообщения
   слов ФИЛЬМ/СЕРИАЛ следующее слово искать в базе и предлагать ссылку)
   **5) добавить новый экран с "все фильмы"**
   **6) исправтиь баг в чате с многосообщениями от пользователей**
   **7) исправить баг с фильмами (отображаются старые фильмы от другого пользователя так не должно быть при перезаходе)
   **
   **8) сделать отдельную страницу (все кино) со списком всех фильмов от пользователей чтобы было видно их оценки и
   поиск уже
   глобальный
   по всем добавленным фильмам**
   **9) сделать страницу со всеми добавленными актерами и каждый ведет на списрк фильмов где указаны только фильмы с
   участием
   этого актера**
